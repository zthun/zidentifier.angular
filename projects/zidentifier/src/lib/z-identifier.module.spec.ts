import { ZIdentifierModule } from './z-identifier.module';

describe('ZIdentifierModule', () => {
  it('should be created.', () => {
    // Arrange
    // Act
    const target = new ZIdentifierModule();
    // Assert
    expect(target).toBeTruthy();
  });
});
